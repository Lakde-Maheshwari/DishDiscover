# Core Django
Django>=4.2,<5.0

# For handling environment variables
python-dotenv>=1.0.0

# PostgreSQL (change if you're using SQLite or MySQL)
psycopg2-binary>=2.9.9

# Django REST framework for APIs
djangorestframework>=3.14.0

# CORS support (for frontend-backend communication)
django-cors-headers>=4.2.0

# JWT Authentication
djangorestframework-simplejwt>=5.3.0

# For static files in production (if you're deploying to Heroku or similar)
whitenoise>=6.6.0

# Pillow for image processing (if your project uses ImageField)
Pillow>=10.0.0

# Optional: Celery for async tasks (add if needed)
# celery>=5.3.4
# django-celery-beat>=2.5.0

# Optional: Channels for real-time features
# channels>=4.0.0

# Optional: if using tailwind with django-tailwind
# django-tailwind>=3.5.0

# Optional: if using drf-yasg for API documentation
# drf-yasg>=1.21.7

